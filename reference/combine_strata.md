# Function to combine strata via interaction if strata is passed as a vector

Function to combine strata via interaction if strata is passed as a
vector

## Usage

``` r
combine_strata(x, strata)
```
